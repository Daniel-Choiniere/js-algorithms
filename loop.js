var cohortFiveNames = ['Paul', 'Dan', 'Brett', 'Josh', 'Tom', 'Christian', 'Joe', 'Mitch', 'Mellissa','Nathan', 'Alejandro'];

 //create a loop that parses through each name and adds a "hello" to each name.
    for (i=0; i<cohortFiveNames.length; i++) {
        console.log("Hello " + cohortFiveNames[i] + "!")
    }